<% title(@project.name, "Project") %>
<%= @project.name %>
<%= link_to "Edit Project", edit_project_path(@project) %> |
<%= link_to "Delete Project", @project, method: :delete, confirm: "Are you sure?" %><br />
<h1><%= link_to "New Ticket", new_project_ticket_path(@project) %></h1>
