<%= form_for @project do |f| %>
  <% if @project.errors.any? %>
    <h1><%= pluralize @project.errors.count, "error" %> forbiddent this project to be created.</h1>
    <ul>
      <% @project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
    </ul>
    <% end %>
<p>
<%= f.label :name %><br />
<%= f.text_field :name %>
</p>
<p><%= f.submit %></p>
<% end %>
